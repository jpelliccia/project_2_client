{{#each sneakers as |sneaker|}}
  <tr>
    <th scope="row">{{sneaker.id}}</th>
    <!-- <td>{{sneaker.brand.id}}</td> -->
    <td>{{sneaker.name}}</td>
    <td>{{sneaker.color}}</td>
    <td>{{sneaker.release_date}}</td>
  </tr>
{{/each}}
